import { RecipeCategoryPipe } from './recipe-category.pipe';

describe('RecipeCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
